export default class UI {
    constructor(ctx, canvas) {
        this.ctx = ctx;
        this.canvas = canvas;
    }
    
    drawRoundStart(roundNumber) {
        this.ctx.drawImage(, this.canvas.width / 2 - 100, 50, 200, 100);
    }
}